---
run:
  timeout: 10m
  modules-download-mode: readonly

linters:
  disable-all: true
  enable:
    - bodyclose
    - gofmt
    - goimports
    - gosec
    - gosimple
    - govet
    - ineffassign
    - noctx
    - staticcheck
    - typecheck
    - unused
    - whitespace

linters-settings:
  goimports:
    local-prefixes: github.com/ricoberger/echoserver
